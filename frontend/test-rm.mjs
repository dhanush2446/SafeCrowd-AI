import React from 'react'; import { renderToString } from 'react-dom/server'; import ReactMarkdown from 'react-markdown'; try { const html = renderToString(React.createElement(ReactMarkdown, { children: 'Hello **world**' })); console.log('SUCCESS:', html); } catch (e) { console.error('ERROR:', e); }
